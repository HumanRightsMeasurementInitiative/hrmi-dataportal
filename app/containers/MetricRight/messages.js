/*
 * MetricRight Messages
 *
 * This contains all the text for the MetricRight container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'hrmi.containers.MetricRight';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MetricRight container!',
  },
});
