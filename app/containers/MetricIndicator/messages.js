/*
 * MetricIndicator Messages
 *
 * This contains all the text for the MetricIndicator container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MetricIndicator';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MetricIndicator container!',
  },
});
