/*
 * PathPeople Messages
 *
 * This contains all the text for the PathPeople container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'hrmi.containers.PathPeople';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'header',
  },
  availability: {
    id: `${scope}.availability`,
    defaultMessage: 'availability',
  },
});
