/*
 * Metric Messages
 *
 * This contains all the text for the Metric container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Metric';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Metric container!',
  },
});
