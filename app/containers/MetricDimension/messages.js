/*
 * MetricDimension Messages
 *
 * This contains all the text for the MetricDimension container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MetricDimension';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MetricDimension container!',
  },
});
