/*
 * Close Messages
 *
 * This contains all the text for the Close component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'hrmi.components.Close';

export default defineMessages({
  label: {
    id: `${scope}.label`,
    defaultMessage: 'Back',
  },
});
